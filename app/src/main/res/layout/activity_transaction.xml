<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_transaction"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    tools:context="com.example.b.expensewatcher.TransactionActivity"
    android:orientation="vertical"
    android:weightSum="1">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:ems="10"
        android:id="@+id/transaction_value"
        android:hint="@string/entervalue"
        android:gravity="center"
        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="date"
        android:ems="10"
        android:layout_marginTop="17dp"
        android:id="@+id/transaction_date"
        android:gravity="center"
        android:layout_below="@+id/transaction_value"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:hint="dd/mm/yyyy" />

    <!--Larger fonts in this listview. Note: item_category.xml is also inflated-->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ListView
            android:layout_width="fill_parent"
            android:layout_height="331dp"
            android:layout_marginTop="47dp"
            android:dividerHeight="10.0sp"
            android:gravity="center"
            android:id="@+id/transaction_categoryList"
            android:choiceMode="singleChoice"
            android:background="@android:color/darker_gray"
            android:headerDividersEnabled="false"
            android:footerDividersEnabled="false"
            android:contextClickable="true"
            android:textAlignment="gravity" />

        <ProgressBar
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:visibility="invisible"
            android:id="@+id/pb_loading_addexpense"
            android:layout_gravity="center"/>

    </FrameLayout>
</LinearLayout>
