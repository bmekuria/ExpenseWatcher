<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_smallmargin">

    <ImageView
        android:id="@id/total_expenses_per_category_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding ="5dp"
        />

    <TextView
        android:id="@+id/total_expenses_per_category_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/activatedBackgroundIndicator"
        android:text="@string/total_expenses_per_category_name" />

    <!--
    An empty view for spacing out the amount and the category-->
    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/total_expenses_per_category_total"
        android:text="@string/total_expenses_per_category_total"/>

</LinearLayout>